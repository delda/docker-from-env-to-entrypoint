This simple repository is a test how pass a variable from `.env` file to `Dockerfile` container.

Example of use:
```
$ git clone https://github.com/delda/docker-from-env-to-entrypoint.git
$ docker-compose up
```
